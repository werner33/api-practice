import React, {useEffect} from 'react';

function StudentCollection({students}) {

    console.log(students)

    return (
        <div>
            
        </div>
    );
}

export default StudentCollection;